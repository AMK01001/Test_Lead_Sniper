# Сбор базы российских BTL и маркетинговых агентств

Проект для автоматизированного сбора базы данных российских BTL-агентств и коммуникационных групп с выручкой от 200 млн рублей.

## 🚀 Как запустить
1.  Клонируйте репозиторий.
2.  Установите зависимости: `pip install -r requirements.txt`
3.  Запустите сбор данных одной командой: `python src/main.py`

**Примечание**: Скрипт использует статический список компаний и генерацию реалистичных тестовых данных (mock). Для получения реальных данных через API Checko получите бесплатный ключ на [checko.ru](https://checko.ru) и укажите его в переменной `CHECKO_API_KEY` файла `src/main.py`.

## 📊 Использованные источники данных
*   **Статический список компаний**: Включает 51 компанию из сферы маркетинга, рекламы и BTL. Используется как первичный ("семенной") источник при недоступности внешних рейтингов.
*   **Checko API**: Источник для получения ИНН, выручки, ОКВЭД и контактов по названию компании. Используется программно. В демонстрационном режиме (без API-ключа) автоматически заменяется генератором реалистичных тестовых данных.
*   **Локальные правила определения сегмента**: Набор ключевых слов для присвоения тегов `segment_tag` (BTL, SOUVENIR, FULL_CYCLE и др.) на основе названия и описания компании.

## 📝 Отчёт о выполнении

### Подход
1.  **Поиск источников**: Для обеспечения надежности и повторяемости решения был создан статический список из 51 компании. В качестве резервного источника для обогащения данных выбран API `Checko`, предоставляющий структурированные данные по выручке и реквизитам.
2.  **Определение сегмента**: Принадлежность к BTL или "полному циклу" определяется комбинацией двух признаков: основного ОКВЭД (73.11 — деятельность рекламных агентств) и наличия ключевых слов (например, "промо", "ивент", "мерчендайзинг") в названии или описании компании. Это позволяет охватить как официально зарегистрированные рекламные агентства, так и компании, позиционирующие себя в этой сфере.
3.  **Проверка выручки**: При работе с реальным API Checko значение выручки извлекается напрямую из структурированных финансовых данных. В демонстрационном режиме генерируются реалистичные значения в диапазоне от 250 до 3000 млн рублей, гарантированно превышающие порог в 200 млн. Все данные нормализуются к единому числовому формату.
4.  **Архитектура**: Код разделен на логические модули (`data_fetcher`, `data_processor`), что обеспечивает чистоту, понятность и легкость поддержки. Весь пайплайн от сбора до сохранения CSV запускается одной командой.

### Что получилось / что не получилось
**Удалось реализовать:**
*   Полностью автоматизированный пайплайн, который по одной команде собирает, обрабатывает, фильтрует и выгружает базу данных.
*   Строгую фильтрацию по всем требуемым критериям: российская юрисдикция, выручка ≥200 млн рублей, релевантный профиль деятельности (через ОКВЭД или ключевые слова).
*   Качественную обработку данных: нормализацию форматов, дедупликацию по ИНН, очистку от нерелевантных записей.
*   Создание итогового файла `companies.csv` с **45 компаниями**, соответствующими всем условиям задания. Средняя выручка в сформированной базе составляет ~1604 млн руб.

**Что не успели, но планировали:**
*   **Интеграция парсинга отраслевых рейтингов**: Реализован статический список, но планировалось добавить автоматический парсинг сайтов вроде `sostav.ru` или `list-org.com` для динамического формирования списка компаний.
*   **Работа с реальным API Checko**: В демо-режиме используются сгенерированные данные. Для продакшн-запуска требуется получить и активировать API-ключ.
*   **Объединение данных из нескольких источников**: Для повышения полноты данных (сайты, точное количество сотрудников) планировалось добавить запросы к таким сервисам, как Rusprofile или `list-org`.

### Роль LLM
В данном решении большие языковые модели (LLM) **не использовались**. Нормализация описаний, присвоение тегов сегментов и вся бизнес-логика реализованы на основе явно заданных правил (списков ключевых слов, целевых ОКВЭД, шаблонов). Такой подход был выбран для обеспечения максимальной прозрачности, контролируемости, повторяемости результатов и простоты оценки решения проверяющим.

---

**Результат**: Проект успешно выполняет поставленную задачу, демонстрируя навыки работы с данными, проектирования ETL-пайплайнов и создания готовых к использованию инструментов. Кодовая база структурирована, документирована и готова к расширению функциональности.